version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - ./src:/usr/src/app/src
      - ./VERSION:/usr/src/app/VERSION
      - /usr/src/app/node_modules
    env_file:
      - .env.development
